# Python 3.11 required (3.10 EOL Oct 2026; 3.12 breaks fairseq deps)

# API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
httpx>=0.27.0
aiofiles>=23.2.1

# ASR — faster-whisper (CUDA, word-level timestamps)
faster-whisper>=1.0.3
ctranslate2>=4.4.0

# PyTorch — install CUDA build separately after venv setup:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.3.0

# 1. Uninstall CPU-only torch if installed
#   pip uninstall torch -y

# 2. Install PyTorch with CUDA 12.1 support (bundles cudart, some CUDA runtime DLLs)
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

# 3. Install nvidia-cublas-cu12 — provides cublas64_12.dll on Windows via pip
#   pip install nvidia-cublas-cu12 nvidia-cuda-runtime-cu12 nvidia-cudnn-cu12

# Transliteration — pure Python, no fairseq, Python 3.11 safe
# Converts Tamil/Telugu/Malayalam/Hindi native script → romanized (ITRANS)
indic-transliteration>=2.3.57

# Translation (Indian langs → English) via HuggingFace transformers
# No separate fairseq needed; uses sentencepiece tokenizer
transformers>=4.44.0
sentencepiece>=0.2.0
sacremoses>=0.1.1

# Export
ffmpeg-python>=0.2.0
